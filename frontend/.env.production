# API que consume el APK de release. Debe coincidir con el dominio declarado
# en android/app/src/main/res/xml/network_security_config.xml, que bloquea
# el trafico en claro hacia el.
VITE_API_BASE_URL=https://api.todogas.com.co
