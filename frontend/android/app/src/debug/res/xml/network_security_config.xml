<?xml version="1.0" encoding="utf-8"?>
<!--
  Solo para builds de depuracion: reemplaza al de src/main, que bloquea el
  trafico en claro. Permite http hacia el runserver del computador, que el
  telefono ve como localhost gracias a `adb reverse tcp:8000 tcp:8000`
  (10.0.2.2 es el mismo computador visto desde el emulador, sin adb reverse).

  El APK de release no incluye este archivo.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.todogas.com.co</domain>
        <domain includeSubdomains="true">todogas-media-dev.s3.us-east-2.amazonaws.com</domain>
    </domain-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
